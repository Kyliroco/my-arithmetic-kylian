image: python:3.11.5

stages:
  - test

unit_tests:
  stage: test
  script:
    - pip install -r requirements.txt
    - coverage run -m pytest
    - coverage report
    - coverage xml
  artifacts:
    reports:
      cobertura: coverage.xml
    expire_in: 1 week

